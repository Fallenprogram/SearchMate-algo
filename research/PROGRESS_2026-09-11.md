# SearchMate progress — 11 September 2026

Internal v37-01 cleared both frozen screens against exact submitted v16.1: short 12W/3D/1L (13.5/16) and full 7W/1D/0L (7.5/8), with zero recorded faults. Its exact package passed local checks. The subsequent platform upload, labelled v6, failed before agent import because its Docker builder could not access `aichessathon/agent-base:latest`. The user contacted the organizers and awaits a reply. No source change is indicated by that log, and platform acceptance is not established.

[V37 release](releases/competition-v6-internal-v37-01/README.md) includes exact source, original weights, assets and the unchanged ZIP, plus portable evidence and all 24 local PGNs. [Model card](releases/competition-v6-internal-v37-01/MODEL_CARD.md) explains CPU training and representation limits. Current known submission remains v16.1 until acceptance is confirmed.

The overnight work retained unsuccessful attempts. V31's original neural residual improved its frozen static accuracy but scored only 3.5/8 in its short screen. Later selective-search and runtime-cost experiments were judged separately. V36 passed short qualification at 12/16 but reached only 4.5/7 full-clock points, making its 6/8 requirement unreachable. V37 changed only the reversible suffix used in v36's score-table context and then passed its own fresh opening screens. Different roots and multiple candidates prevent attributing the W/D/L change to one component alone. No large ranking/Elo guarantee is claimed.

The successful build combines original neural evaluation, guarded late-move reductions, full-board-verified runtime static caching, the revised iteration forecast, and draw-aware reversible-history score-table context. The KQK policy and assets remain inherited unchanged. R82 defense and the uncovered two-bishop conversion question remain explicit gaps.

Native/context, reset, full-game and exact-package results are recorded in the release. The v37 opening queries requested 4.8 million nodes and recorded 4,802,876. Its final review verified 257 frozen/evidence files and all 37 protected artifacts unchanged. No additional training, engine search or competitive campaign was run to prepare this GitHub publication.

A local static package-reader schema problem was corrected in a separate continuation before any package engine request, with failed records retained and no measured retry. It did not change game results or the candidate. The later platform base-image failure is an independent infrastructure event. Both are recorded so the publication does not imply a first-pass platform success.

M1-P001 retained import/reset/cleanup and exact identities. M1-P002 kept static accuracy and selected-position analysis separate from game evidence. M1-P003 bounded explanations and retained failed attempts and useful drawing defenses. This is Recuris-inspired process memory, not a measured causal memory advantage.

The user's final-development boundary is to await successful V37 validation, then consider at most one focused endgame repair if sufficient time remains. No such repair has started or been completed in this update. The delivered v37 bytes stay frozen. No further unrestricted iteration or automated upload is implied by publication.

The public rated inventory remains the verified rounds26–90 snapshot. Newer private/public observations are not silently added as source-attributed rated results. Older dated reports preserve what was known at the time; [current state](CURRENT_STATE.md) supersedes their deployment/status statements. V20 source and ZIP remain unpublished under the prior explicit boundary. Root player, harness and historical releases remain unchanged.
